<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dxsch="clr-namespace:DevExpress.Maui.Scheduler;assembly=DevExpress.Maui.Scheduler"
             xmlns:ViewModels="clr-namespace:StudyN.ViewModels"
             xmlns:dxco="clr-namespace:DevExpress.Maui.Controls;assembly=DevExpress.Maui.Controls"
             Title="Calendar Page"
             IconImageSource="scheduler"
             BackgroundColor="{StaticResource NormalBackgroundColor}"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="true"
             x:Class="StudyN.Views.CalendarPage">
    <ContentPage.BindingContext>
        <ViewModels:ItemsViewModel />
    </ContentPage.BindingContext>
    <Shell.TitleView>
        <StackLayout
            Orientation="Horizontal"
            HorizontalOptions="Center"
            Spacing="5">
            
            <Button Text="Add calendar event"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            BorderWidth="1"
            TextColor="Black"
            BackgroundColor="White"
            Clicked="OnAddCalendarEventClicked"
            ></Button>
            
            <Button Text="Daily"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            BorderWidth="1"
            TextColor="Black"
            BackgroundColor="White"
            Clicked="OnDailyClicked"
            ></Button>
            <Button Text="Weekly"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            BorderWidth="1"
            TextColor="Black"
            BackgroundColor="White"
            Clicked="OnWeeklyClicked"
            ></Button>
            <Button Text="Monthly"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            TextColor="Black"
            BorderWidth="1"
            BackgroundColor="White"
            Clicked="OnMonthlyClicked"
            ></Button>
        </StackLayout>
    </Shell.TitleView>
    <ContentPage.Content>
        <StackLayout>
            <dxsch:DayView x:Name="dayView"
                           DoubleTap="CalenderDoubleTap">
                <dxsch:DayView.DataStorage>
                    <dxsch:SchedulerDataStorage>
                        <dxsch:SchedulerDataStorage.DataSource>
                            <dxsch:DataSource AppointmentsSource="{Binding CalenderItems}">
                                <dxsch:DataSource.AppointmentMappings>
                                    <dxsch:AppointmentMappings 
                                    Id="Id" 
                                    Start="StartTime" 
                                    End="EndTime" 
                                    Subject="Text"
                                    />
                                </dxsch:DataSource.AppointmentMappings>
                            </dxsch:DataSource>
                        </dxsch:SchedulerDataStorage.DataSource>
                    </dxsch:SchedulerDataStorage>
                </dxsch:DayView.DataStorage>
            </dxsch:DayView>
            <dxsch:WeekView x:Name="weekView"
                            DoubleTap="CalenderDoubleTap">
                <dxsch:WeekView.DataStorage>
                    <dxsch:SchedulerDataStorage>
                        <dxsch:SchedulerDataStorage.DataSource>
                            <dxsch:DataSource AppointmentsSource="{Binding CalenderItems}">
                                <dxsch:DataSource.AppointmentMappings>
                                    <dxsch:AppointmentMappings 
                                    Id="Id" 
                                    Start="StartTime" 
                                    End="EndTime" 
                                    Subject="Text"
                                    />
                                </dxsch:DataSource.AppointmentMappings>
                            </dxsch:DataSource>
                        </dxsch:SchedulerDataStorage.DataSource>
                    </dxsch:SchedulerDataStorage>
                </dxsch:WeekView.DataStorage>
            </dxsch:WeekView>
            <dxsch:MonthView x:Name="monthView"
                             DoubleTap="CalenderDoubleTap">
                <dxsch:MonthView.DataStorage>
                    <dxsch:SchedulerDataStorage>
                        <dxsch:SchedulerDataStorage.DataSource>
                            <dxsch:DataSource AppointmentsSource="{Binding CalenderItems}">
                                <dxsch:DataSource.AppointmentMappings>
                                    <dxsch:AppointmentMappings 
                                    Id="Id" 
                                    Start="StartTime" 
                                    End="EndTime" 
                                    Subject="Text"
                                    />
                                </dxsch:DataSource.AppointmentMappings>
                            </dxsch:DataSource>
                        </dxsch:SchedulerDataStorage.DataSource>
                    </dxsch:SchedulerDataStorage>
                </dxsch:MonthView.DataStorage>
            </dxsch:MonthView>

            <dxco:DXPopup x:Name="Popup">
                <StackLayout WidthRequest="300" BackgroundColor="AliceBlue">
                    <Label Text="Add a calendar event!"
                        TextColor="{StaticResource NormalText}"
                        Margin="5, 5"/>

                    <Entry x:Name="Description"
                        MaxLength="200"
                        Style="{StaticResource CommonEntryStyle}"
                        Placeholder="Description"
                        Text="" />

                    <Label Text="Start Time YYYY/MM/DD/HH/MM/SS "></Label>
                    <Entry x:Name="StartTime"
                        MaxLength="200"
                        Style="{StaticResource CommonEntryStyle}"
                        Placeholder="YYYY/MM/DD/HH/MM/SS"
                        Text="" />

                    <Label Text="End Time YYYY/MM/DD/HH/MM/SS "></Label>
                    <Entry x:Name="EndTime"
                        MaxLength="200"
                        Style="{StaticResource CommonEntryStyle}"
                        Placeholder="YYYY/MM/DD/HH/MM/SS" />

                    <Button Text="Add"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"
                        BorderColor="Black"
                        BorderWidth="1"
                        TextColor="Black"
                        BackgroundColor="White"
                        Clicked="OnPopupAddButtonClicked"></Button>

                </StackLayout>
            </dxco:DXPopup>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>



<!--<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dxsch="clr-namespace:DevExpress.Maui.Scheduler;assembly=DevExpress.Maui.Scheduler"
             xmlns:ViewModels="clr-namespace:StudyN.ViewModels"
             Title="Calendar Page"
             IconImageSource="scheduler"
             BackgroundColor="{StaticResource NormalBackgroundColor}"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="true"
             x:Class="StudyN.Views.CalendarPage">
    <ContentPage.BindingContext>
        <ViewModels:TaskDataViewModel />
    </ContentPage.BindingContext>
    <Shell.TitleView>
        <StackLayout
            Orientation="Horizontal"
            HorizontalOptions="Center"
            Spacing="5">
            <Button Text="Daily"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            BorderWidth="1"
            TextColor="Black"
            BackgroundColor="White"
            Clicked="OnDailyClicked"
            ></Button>
            <Button Text="Weekly"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            BorderWidth="1"
            TextColor="Black"
            BackgroundColor="White"
            Clicked="OnWeeklyClicked"
            ></Button>
            <Button Text="Monthly"
            VerticalOptions="Center"
            HorizontalOptions="Center"
            BorderColor="Black"
            TextColor="Black"
            BorderWidth="1"
            BackgroundColor="White"
            Clicked="OnMonthlyClicked"
            ></Button>
        </StackLayout>
    </Shell.TitleView>
    <ContentPage.Content>
        <StackLayout>
            <dxsch:DayView x:Name="dayView">
                <dxsch:DayView.DataStorage>
                    <dxsch:SchedulerDataStorage>
                        <dxsch:SchedulerDataStorage.DataSource>
                            <dxsch:DataSource AppointmentsSource="{Binding CalendarTasks}">
                                <dxsch:DataSource.AppointmentMappings>
                                    <dxsch:AppointmentMappings 
                                    Id="Id" 
                                    Start="StartTime" 
                                    End="DueDate" 
                                    Subject="Description"
                                    LabelId="Id"
                                    Location="Location"
                                    />
                                </dxsch:DataSource.AppointmentMappings>
                            </dxsch:DataSource>
                        </dxsch:SchedulerDataStorage.DataSource>
                    </dxsch:SchedulerDataStorage>
                </dxsch:DayView.DataStorage>
            </dxsch:DayView>
            <dxsch:WeekView x:Name="weekView">
                <dxsch:WeekView.DataStorage>
                    <dxsch:SchedulerDataStorage>
                        <dxsch:SchedulerDataStorage.DataSource>
                            <dxsch:DataSource AppointmentsSource="{Binding CalendarTasks}">
                                <dxsch:DataSource.AppointmentMappings>
                                    <dxsch:AppointmentMappings 
                                    Id="Id" 
                                    Start="StartTime" 
                                    End="DueDate" 
                                    Subject="Description"
                                    LabelId="Id"
                                    Location="Location"
                                    />
                                </dxsch:DataSource.AppointmentMappings>
                            </dxsch:DataSource>
                        </dxsch:SchedulerDataStorage.DataSource>
                    </dxsch:SchedulerDataStorage>
                </dxsch:WeekView.DataStorage>
            </dxsch:WeekView>
            <dxsch:MonthView x:Name="monthView">
                <dxsch:MonthView.DataStorage>
                    <dxsch:SchedulerDataStorage>
                        <dxsch:SchedulerDataStorage.DataSource>
                            <dxsch:DataSource AppointmentsSource="{Binding CalendarTasks}">
                                <dxsch:DataSource.AppointmentMappings>
                                    <dxsch:AppointmentMappings 
                                    Id="Id" 
                                    Start="StartTime" 
                                    End="DueDate" 
                                    Subject="Description"
                                    LabelId="Id"
                                    Location="Location"
                                    />
                                </dxsch:DataSource.AppointmentMappings>
                            </dxsch:DataSource>
                        </dxsch:SchedulerDataStorage.DataSource>
                    </dxsch:SchedulerDataStorage>
                </dxsch:MonthView.DataStorage>
            </dxsch:MonthView>
        </StackLayout>
        </ContentPage.Content>
</ContentPage>-->
    
