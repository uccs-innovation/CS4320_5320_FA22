<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
         xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
         xmlns:dxn="clr-namespace:DevExpress.Maui.Navigation;assembly=DevExpress.Maui.Navigation"
         xmlns:local="clr-namespace:StudyN.Views"
         Style="{StaticResource ShellStyle}"
         xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
         ios:Page.UseSafeArea="true"
        x:Class="StudyN.Views.MainPage">
    <TabBar>
        <Tab Title="Home" Icon="icons8home">
            <ShellContent ContentTemplate="{DataTemplate local:HomePage}" />
        </Tab>
        <Tab Title="Calendar" Icon="scheduler">
            <ShellContent ContentTemplate="{DataTemplate local:CalendarPage}" />
        </Tab>

        <Tab Title="Tasks" Icon="grid">
            <ShellContent ContentTemplate="{DataTemplate local:TaskPage}"/>
        </Tab>
        
        <Tab Title="Analytics" Icon="charts">
            <ShellContent ContentTemplate="{DataTemplate local:AnalyticsPage}" />
        </Tab>
        
        <Tab Title="Categories" Icon="popup">
            <ShellContent ContentTemplate="{DataTemplate local:CategoriesPage}"/>
        </Tab>
        <Tab Title="Settings" Icon="info">
            <ShellContent ContentTemplate="{DataTemplate local:SettingsPage}" />
        </Tab>
        <Tab Title="About us" Icon="info">
            <ShellContent ContentTemplate="{DataTemplate local:AboutUs}" />
        </Tab>
        <ShellContent Route="HomePage">
            <local:HomePage/>
        </ShellContent>

        


    </TabBar>

</Shell>